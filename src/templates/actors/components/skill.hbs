<div class="mod">
    <span class="operator">+</span>
    <span class="val">{{derived skill.mod }}</span>
</div>
<a class="name" data-action="roll-skill">
    <span>{{ localize skill.label }}</span>
</a>
<div class="attribute">
    <span>{{ localize skill.attributeLabel}}</span>
</div>

{{#if pips}}
    <div class="skill-pips ranks">
        {{#if editable}}
            {{#if legacyMode}}
                <a data-tooltip="{{localize "COSMERE.Actor.Sheet.Skills.AdjustTooltip" skill=(localize skill.label)}}">
            {{else}}
                <a data-tooltip="{{localize "COSMERE.Actor.Sheet.Skills.SetTooltip" skill=(localize skill.label)}}">
            {{/if}}
        {{else}}
            <div>
        {{/if}}
        <ul class="pip-list">
            {{#each (numberRange skill.ranks.base)}}
                <li class="pip active">
                    <div {{#if ../editable}}data-action="adjust-skill-rank" data-index='{{@index}}'{{/if}}></div>
                </li>
            {{/each}}
            {{#each (numberRange skill.ranks.bonus)}}
                <li class="pip active bonus">
                    <div data-action="adjust-skill-rank" data-index='{{add @index ../skill.ranks.base}}'></div>
                </li>
            {{/each}}
            {{#each (numberRange (sub 5 (add skill.ranks.base skill.ranks.bonus)))}}
                <li class="pip{{#if (gt (add (add @index 1) (add ../skill.ranks.bonus ../skill.ranks.base)) ../maxSkillRank)}} locked{{/if}}">
                    <div {{#if ../editable}}data-action="adjust-skill-rank" data-index='{{add @index (add ../skill.ranks.bonus ../skill.ranks.base)}}'{{/if}}></div>
                </li>
            {{/each}}
        </ul>
        {{#if editable}}
            </a>
        {{else}}
            </div>
        {{/if}}
    </div>
{{/if}}
